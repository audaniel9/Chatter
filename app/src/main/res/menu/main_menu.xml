<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:icon="@drawable/ic_search_icon" app:showAsAction="always"
        android:id="@+id/menuSearch">
    </item>

    <item android:icon="@drawable/ic_refresh_icon" app:showAsAction="always"
        android:id="@+id/menuRefresh"/>

    <item android:title="Settings" app:showAsAction="never"
        android:id="@+id/menuSettings"></item>

    <item android:title="About" app:showAsAction="never"
        android:id="@+id/menuAbout">
    </item>

    <item android:title="Sign Out" app:showAsAction="never"
        android:id="@+id/menuSignOut">
    </item>
</menu>